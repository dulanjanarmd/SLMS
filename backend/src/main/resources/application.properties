# Application Name
spring.application.name=SLIIT Library Management System

# Server Configuration
server.port=8080

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/sliit_library_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=dul.12345
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration - Auto create/update tables
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
app.jwt.secret=c2xpaXQtbGlicmFyeS1tYW5hZ2VtZW50LXN5c3RlbS1zZWNyZXQta2V5LTIwMjQtdmVyeS1zZWN1cmUtYW5kLWxvbmc=
app.jwt.expiration-ms=86400000

# File Upload Configuration
spring.servlet.multipart.max-file-size=500MB
spring.servlet.multipart.max-request-size=500MB

# Upload Directory
app.upload.dir=uploads/membership-photos

# Email Configuration (for notifications)
# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# spring.mail.username=your-email@gmail.com
# spring.mail.password=your-app-password
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true

# Fine Configuration (LKR 5 per day as per SRS)
library.fine.rate-per-day=5.00
library.fine.max-cap-percentage=150

# Loan Policies (as per SRS Section 3.3)
library.loan.undergraduate.max-books=4
library.loan.undergraduate.max-days=14
library.loan.postgraduate.max-books=6
library.loan.postgraduate.max-days=21
library.loan.faculty.max-books=10
library.loan.faculty.max-days=30
library.loan.max-renewals=2

# CORS Configuration
cors.allowed-origins=http://localhost:5173,http://localhost:3000,http://localhost:3001

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.sliit.library=DEBUG